data:
  dataset_path: "delhi_pm25_lstm_dataset.csv"
  target_column: "pm2_5"
  train_split: 0.8
  lookback_window: 24

train:
  batch_size: 32
  learning_rate: 0.001
  epochs: 50

model:
  hidden_dim: 64
  num_layers: 2
  dropout: 0.2
  nhead: 4

mlflow:
  experiment_name: "delhi_pm25_test"